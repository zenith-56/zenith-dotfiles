#!/usr/bin/env bash
# =============================================================================
# Zenith-Dotfiles - Set Volume
# =============================================================================

[ -z "$1" ] && exit 1
command -v pamixer >/dev/null 2>&1 || exit 1
pamixer --set-volume "$1" 2>/dev/null || exit 1
