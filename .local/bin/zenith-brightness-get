#!/usr/bin/env bash
# =============================================================================
# Zenith-Dotfiles - Get Brightness
# =============================================================================

command -v brightnessctl >/dev/null 2>&1 || { echo "0"; exit 1; }
brightnessctl get 2>/dev/null | sed 's/.*(\([0-9]*\)%).*/\1/' || echo "0"
