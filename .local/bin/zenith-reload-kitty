#!/usr/bin/env bash
# =============================================================================
# Zenith-Dotfiles - Reload Kitty (live)
# =============================================================================

pids=$(pidof kitty 2>/dev/null)

if [ -n "$pids" ]; then
    for pid in $pids; do
        kill -SIGUSR1 "$pid" 2>/dev/null || true
    done
fi
