#!/usr/bin/env bash
# =============================================================================
# Zenith-Dotfiles - Screenshot Region
# =============================================================================

command -v awww >/dev/null 2>&1 || { echo "awww not installed"; exit 1; }
awww shot -s --no-notification
